<h1>show</h1>

 <p>name</p>
  <%= link_to @user.name, user_path(@user) %>

<h2>タイトル</h2>
<%= link_to @book.title, book_path(@book) %></p>

<h2>本文</h2>
<p><%= @book.body %></p>
<%= link_to "edit", edit_book_path(@book) %>


<h1>New book</h1>
<%= form_with model: @new_book do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>